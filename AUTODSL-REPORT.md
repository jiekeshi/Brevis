# 自扩展 DSL：我们在论文之上加了什么，以及怎么验证的

草稿，供讨论修改。所有数字都是本仓库实测，可复现命令与哈希见文末。
分支 `autoDSL`，实验日志在 [`doc/progress.md`](doc/progress.md)，设计文档在
[`doc/autodsl.md`](doc/autodsl.md)。

**这些是 development measurement，不是 `eval/` 那套 audited 结果**，正式发表前
需要走 `eval/` 的 formal 路径重测一遍。

---

## 0. 一句话结论

在现有算子集之上做「宏级别的自扩展」是**有效的但有明确上界**：在冻结的未见
checkpoint 上净减 **0.044%**（逐比特正确、规划耗时反而下降），而这个方向的
**天花板本身就是 0.044%**——我们把项目产生过的所有宏最优组合起来，也只有这个数。
因此限制因素是**算子集的表达能力**，不是搜索策略。

这个「我们测出了自己方法的上界」是可以写进论文的正面贡献，它能挡掉
"为什么不试更强的搜索" 这个必然会被问到的问题。

---

## 1. 我们在原始论文之上加了什么

原始 Brevis：算子集固定，A\* 在这个固定语法里为每个 tensor 搜索一个可逆程序，
PHOG 先验只决定**展开顺序**，不改变字节目标。

我们加的东西分三层。

### 1.1 引擎层：可学习的宏（macro）

一条宏是**由现有算子构成的、叶子为洞或终结符的子树**，作为**单一产生式**提供给
A\*：

```json
{"name": "float_fields_delta_exp",
 "body": {"op": "split_float", "children": [
   {"op": "hole"},
   {"op": "diff_mod", "children": [{"op": "hole"}]},
   {"op": "hole"}]}}
```

三条性质是设计的核心，都由构造保证并有测试守护：

**宏不可能藏数据。** body 里只有算子和洞，没有载荷。那种「把整个 tensor 塞进规则
定义、程序只剩三个字节」的退化解在这里不是被惩罚，而是**不可表示**。每个字节仍然
只能从终结符载荷出去，并被完整计入。

**宏不可能扩大可表达集合。** body 的每个节点都要在它落到的位置通过
`legalProductions` 检查——位宽守卫、深度预算、`--disable-op` 全部照常生效。所以宏
只能构造出原语文法本身也能构造的程序；一次消融如果移除了 `zigzag`，也会移除所有
含 `zigzag` 的宏。

**宏不可能改变归档。** 引擎在**序列化之前**把宏展开成原语，所以 `.brv` schema、
字节码、解码器完全不变；带库和不带库写出的归档，在两者选中同一程序时**逐字节
相同**。解压端从不知道宏的存在。

宏改变的只有一件事：**在有限展开预算下，搜索能够到达哪些程序。**

新增/改动：`src/macro.zig`（库的 schema、解析、校验）、`src/search.zig` 的
`fitMacroBody`、`src/report.zig` 的算子表导出与逐 tensor 宏归因、
`src/main.zig` 的 `--macros` 参数。

### 1.2 关键的代价模型修正（这一条建议单独写进论文）

一条宏在 A\* 里该怎么计费？我们最初按「它展开后各节点代价之和」计费，理由是宏不该
获得未挣得的排序优势。**这个理由是错的。**

A\* 按 `f` 展开，所以一条 2 节点的宏若按 `2 × floor` 计费，就排在**整个单产生式前
沿之后**，而那个前沿里每一个被弹出的节点又会压入约 15 个子节点。256 次展开之内，
宏永远轮不到。

正确的读法也更符合 MDL 的本意：**宏是扩展文法 `G ∪ M` 中的一个符号，因此计一次
产生式**——这正是「抽象」的定义，它缩短了使用它的程序的描述长度。字节目标
`g_bytes + lowerBound` 完全不受影响，所以「两种代价相互独立」这条不变量仍然成立。

实测（SmolLM，同一个手写种子库）：

| 计费方式 | 归档字节 | vs 无宏 | 选中宏构造程序的 tensor |
| --- | ---: | ---: | ---: |
| 按展开节点数 | 171,932,437 | **+92,674** ❌ | **0 / 483** |
| 按一次产生式 | 171,835,441 | **−3,988** | **210 / 483** |

这是整个项目里单个影响最大的发现：在修正之前，所有宏（包括 LLM 提出的三条合理
提议）**从未被选中过一次**，看上去像是「宏没用」，实际是计费方式让它们不可达。

### 1.3 外层循环：LLM 提议 + 确定性门禁（`autodsl/`）

```
内层   A* 在当前 DSL 中为每个 tensor 搜索程序
外层   挖掘 + LLM 提议新宏 → 验证 → 度量 → 保留或拒绝
```

**模型只负责「提议」。** 它看到的只有：从 `brevis config` 导出的算子表、一行语义
说明、搜索预算、**一个 develop checkpoint** 的测量证据、当前库、已挖掘形状、已拒绝
列表。它不能读源码、不能提出新原语、不能修改搜索代价、不能修改自己的 prompt。
输出必须是受限 JSON，arity 与算子名在解析期校验。

**决定去留的全是确定性的**，按代价从低到高排列，第一个失败即停：

1. **结构** — arity、已知算子、不含宽度相关 arity 的算子、名字与形状唯一
2. **引擎接受** — 库能加载且每条宏存活
3. **触发** — 真的有 tensor 选中了这条宏构造的程序（未触发的宏不是中性的，它会稀释
   固定预算）
4. **逐比特** — 写真实 `.brv` 并 `brevis verify`
5. **有收益** — develop 降幅超过 margin、validation 涨幅在容差内、规划耗时
   ≤ 1.25×
6. **不毁掉任何单个模型** — 语料总和会掩盖某个文件被严重伤害，单文件损失不得超过
   自身 0.2%

第 6 条是被数据逼出来的：某次运行语料总量减少 7.36 MB，其中一个 Qwen 分片却
**大了 4.43 MB**。只看总和的门禁会放行。

### 1.4 三层数据划分（一个必须交代的方法论修正）

我们最初只有 develop / holdout 两层，而 holdout **参与了每一个候选的接受决策**
（作为非回退否决票，约 12 次），并且被 `verdict` 读过两次。**那是 validation，不是
test。** 现在三层，代码强制：

| 层 | 模型 | 角色 |
| --- | --- | --- |
| develop | SmolLM (I8+BF16)、BERT (F32)、ViT (F32) | 被挖掘、**LLM 只看到其中一个**、必须改善 |
| validation | TinyLlama (BF16) | 接受时的否决票 → 属于训练回路 |
| **test** | Whisper (F16)、SDXL (F16, 4 组件)、Qwen3-8B (BF16, 5 分片) | **只有 `verdict` 读**，每次接触追加到 `runs/test-log.jsonl` |

「提议器只能看到一个 develop checkpoint」现在是一个**可测试的性质**
（`loop.proposer_evidence_source`），而不是一个约定。

---

## 2. 我们做了哪些实验

### 2.1 诊断实验：搜索到底受限于什么

**预算曲线**（`experiments.py budget-curve`）：对原始 DSL 和宏 DSL 分别扫描
64…4096 次展开预算，记录归档字节与规划耗时。这个实验决定了贡献的**性质**：

- 若原始 DSL 加大预算后也能找到同样的程序 → 宏只是**搜索加速**
- 若在同等甚至更大 wall-time 下原始 DSL 仍找不到 → 宏是更强的**可达性**抽象
- 只有加入现有原语无法表达的新算子，才算真正扩大**表达能力**

结果**逐模型不同**：

| 模型 | 原始 DSL 扫描内最优 | 宏 @256 | 结论 |
| --- | ---: | ---: | --- |
| SmolLM (I8+BF16) | 171,835,356 @512 | **171,726,489** | 4096 内从未追平 → **可达性** |
| BERT (F32) | 365,681,984 @4096 | 366,000,885 | @512 追平，且 wall-time 更低 → 至多加速 |
| ViT (F32) | 288,079,353 @512 | 288,080,233 | 同上 |

两个被标题掩盖的事实：**低预算下效应极大**（e=64 时 BERT −10.9%、ViT −10.6%），
以及**更多搜索不单调**（原始 DSL 从 512→2048 在两个模型上都变差），所以「给
baseline 更多预算」不是一个干净的对照。

### 2.2 收益分布：广泛还是集中

`experiments.py per-tensor` 逐张量对比带宏与不带宏：

| 模型 | dtype | 宏触发张量 | 改善/变差 | 改善中位数 | top-5 集中度 | 总计 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| TinyLlama | BF16 | **174/201** | 175/15 | **8,094 B** | **15.2%** | −2,121,427 |
| SmolLM | I8+BF16 | 212/483 | 196/5 | 7 B | 98.9% | −112,940 |
| BERT | F32 | **0** | 166/31 | 7 B | 46.2% | −5,984 |
| ViT | F32 | **0** | 156/34 | 7 B | 26.6% | −5,070 |

只有在纯 BF16 模型上收益是**广泛的**。F32 上宏**从不触发**，那些小差值是重排序噪
声，且 31/34 个张量反而变差。I8 张量收益恰好为 0。

**结论：学到的是一条 BF16 浮点字段规则，不是通用规则。** 这一条必须写进
limitation。

### 2.3 冻结测试集：唯一能支撑泛化主张的测量

三个从未被挖掘、从未被提议器看到、从未参与任何接受决策的 checkpoint 家族：
Whisper (F16, 语音)、SDXL (F16, 扩散, 4 组件)、Qwen3-8B (BF16, 5 分片)，
共 10 个文件 26.4 GB。

```
基线 19,390,217,369 → 19,381,608,960
净减 8,608,409 字节 = 0.0444%（已扣除规则库序列化开销）
10/10 文件逐比特正确：写真实 .brv → verify → 全量解压
规划耗时 0.98×（更快）
```

**逐文件必须一起报告**（10 个里 3 个变差）：

| 文件 | Δ | 最差单张量 |
| --- | ---: | ---: |
| Qwen3-8B shard 1 | +4,429,751 | +2,179,130 |
| Whisper (F16) | +165,305 | +258,575 |
| SDXL text_encoder_2 | +91,792 | +127,323 |
| 其余 7 个文件 | −13,295,257 | — |

### 2.4 天花板实验：这个方向还剩多少

**构想：** 不问「哪种搜索更好」，而问「总共能拿到多少」。把这个项目**曾经产生过
的全部 9 条宏**（挖掘的、LLM 提议的、变异的，包括后来被否决的库里的）并成候选池，
直接做前向选择——**不需要任何搜索去找，等于直接给答案**。这是宏级自扩展在当前算子
集上的乐观上界。

前向选择停在两条宏：

```
split_field(rans, split_field(rans, bitpack))      [LLM 提议]
split_float(?, rans, split_field(?, bitpack))      [变异得到]
```

| 库 | test Δ | 占比 | 占天花板 |
| --- | ---: | ---: | ---: |
| **天花板：所有宏的最优子集** | **−8,608,409** | **0.0444%** | 100.0% |
| 全自动搜索（30 次评估，单种子） | −8,430,498 | 0.0435% | **97.9%** |
| 最早手工组装的 2 宏库 | −7,357,344 | 0.0379% | 85.5% |
| 全自动贪心 | −5,882,844 | 0.0303% | 68.3% |
| 纯挖掘 | +7,954,920 | — | **比什么都不做更差** |

**这是最重要的一个实验。** 全自动流程已经拿到天花板的 97.9%，意味着再好的搜索策
略最多只能再拿 2.1%（约 180 KB）。**瓶颈在算子集，不在搜索。**

宏重排的是「有限搜索到达哪个程序」，无法改变占归档 **99.87%** 的残差比特如何建模
（程序字节码只占 0.13%，规则库本身占 0——序列化前就展开了）。

### 2.5 消融：搜索机制本身贡献了什么

**关掉 LLM 的种群搜索**：花光 30 次评估预算，develop −16,481 / validation
−41,092；而**纯挖掘**是 −16,531 / −43,084。**变异、交叉、随机采样在挖掘种子上的
最终落点就是挖掘本身。**

**多轮细化**：minimax 打分下每个种子的 best-by-round 在第一轮之后全是平的。收益来
自单次好提议，不是逐轮细化。

**一个致命的方法论发现**：同一个种子，greedy 两次跑出 **−38,042** 和 **+0**。
**LLM 的非确定性方差比被测效应本身大**，所以任何单次运行的 arm 对比都不构成证据。
我们随后改用**固定宏池 + 关闭 LLM** 的确定性对照来隔离选择策略本身。

### 2.6 受控对照：把提议器的方差移出比较

2.5 最后那个发现（同种子 greedy 跑出 0 和 −38,042）意味着带 LLM 的 arm 对比毫无
证据力。**构想：给两个臂同一个固定的 9 宏池、完全不调 LLM**，它们只在「怎么选、
怎么组合」上不同。这是确定性的，也是唯一能回答「多轮组合搜索是否优于逐条贪心」的
干净实验。

| 臂 | develop Δ | validation Δ | 评估次数 | 轮数 | 宏数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| **search**（前向选择 + 组合 + 丢弃） | **−752,660** | −2,132,909 | 30 | 7 | 3 |
| greedy（逐条按序接受） | −743,193 | −2,125,307 | **9** | 10 | 2 |

search 确实找到了最优解——**−752,660 正是 2.4 天花板库的 develop 值**。但
**贪心用 30% 的预算拿到了 98.7% 的收益**。

**这条实验定案了搜索一节的去留。** 在固定池上，多轮组合搜索相对逐条贪心的增量是
1.3%，代价是 3.3 倍的评估预算。加上 2.5 的三条消融（机制零贡献、无多轮细化、带
LLM 时对比被噪声淹没），**没有任何一个角度支持把种群/进化搜索作为贡献写出来**。

---

## 3. 建议如何写进论文

**建议写：** 1.1（宏机制与三条不变量）、1.2（代价模型，单独成段）、2.3（冻结测试
集结果）、2.4（天花板，作为正面的边界贡献）、2.2 的 limitation。

**建议不写：** 进化/种群搜索。四条独立证据：(a) 2.4 全自动流程已达天花板 97.9%；
(b) 2.5 关掉 LLM 后种群搜索落点等于纯挖掘；(c) 2.5 无多轮细化，且带 LLM 时对比被
噪声淹没（同种子 0 vs −38,042）；(d) 2.6 受控对照下贪心用 30% 预算拿到 98.7%。

如果一定要提，一句话即可：*"我们也试过在库空间上做种群搜索与组合评估；在受控对照
下它相对逐条贪心接受的增量为 1.3%，代价是 3.3 倍的评估预算，因此本文采用后者。"*
这样反而显得实验充分。

**必写的 limitation：**

1. 学到的是 BF16 浮点字段规则；F32 上从不触发，F16 上让 Whisper 变差
2. 冻结测试集只有 3 个模型族 / 10 个文件
3. 效应量 0.044%，且已证明是该方向的上界
4. 尚未加入新原语——那是另一类工作，有真正的可逆性证明义务

---

## 4. 复现信息

| | |
| --- | --- |
| 分支 / 提交 | `autoDSL`，`061318f` → 最新 |
| 工具链 | Zig 0.16.0, Python 3.12.4, `source setup_env.sh` |
| 节点 | c170.nibi.sharcnet, Xeon 6972P, `--jobs 12` |
| 二进制 | sha256 `583ac71fd32c1de0c7be77aca3c1ed574323a1bc6cbd36cc407a9a5fe1772f15` |
| 确定性 | search/codec 无 RNG；重复运行与**跨线程数**逐字节相同 |
| checkpoint | repo + revision + 逐文件 sha256 固定在 `eval/models-tiered.json` |
| 测试 | 45 Zig + 190 Python (autodsl) + 13 (tools) 全通过 |

```bash
source setup_env.sh && zig build -Doptimize=ReleaseFast
cd autodsl
python3 loop.py bootstrap --top 8            # 纯挖掘，不调模型
python3 loop.py propose --rounds 1 --wanted 3
python3 loop.py verdict --tier test          # 冻结测试集判定
python3 experiments.py --run exp budget-curve   # 加速 vs 可达性
python3 experiments.py --run exp per-tensor     # 收益分布
python3 ceiling.py                              # 天花板
python3 search.py --run controlled --pool runs/pool.json --arms greedy,search
```

成本口径（避免「0.98× 规划」被读成免费）：宏只花一次 A\* pop，但
`fitMacroBody` 会对 body 的每个节点跑 `ops.forward` 并建直方图，单次 pop 更贵——
0.98× 是**已经付过这笔钱之后**的数字。规则发现阶段离线不计入：约 40 次引擎调用、
数次模型调用、数小时 wall clock。
