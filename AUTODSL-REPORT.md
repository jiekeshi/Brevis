# 让 Brevis 自动扩展它的可逆算子语法

本文记录我们在 Brevis 之上新增的一层工作：让系统自己向可逆算子语法中添加规则，并用
确定性的验证与度量决定哪些规则值得保留。

阅读本文只需要了解 Brevis 主体：为每个 tensor 在一个固定的可逆算子集上用 A\* 合成
一段程序，归档中存放程序与叶子数据，解压端不做搜索、直接执行程序。

代码在分支 `autoDSL`，新增 `src/macro.zig` 与目录 `autodsl/`。完整实验日志见
`doc/progress.md`，设计文档见 `doc/autodsl.md`。

---

## 1. 动机

Brevis 的压缩能力来自程序合成，而程序合成的能力边界由算子集决定。这一点在原文中是
隐含的：A\* 搜索的是"给定语法内最短的程序"，PHOG 先验只影响展开顺序，不改变字节
目标。因此

> 语法能表达的规律 ⟹ 压缩器能够发现的规律

语法里没有对应的规则，再好的搜索也找不到。原文也把"更广的 DSL"和"跨 tensor 程序"
列为未来方向。

于是问题是：**能不能让系统自己扩展这个语法？** 如果可以，那么每遇到一类新的权重
结构，压缩器不必等人去手写算子，而是能从数据中提出候选规则、自行验证、自行决定是否
纳入。

这个想法有一个必须先处理的风险。如果允许系统随意添加规则，最优解是退化的：

```
def GenerateTensor():
    return [这里直接写入整个 tensor]
```

调用它只需要几个字节，但数据全都藏进了规则定义。所以"自扩展语法"的第一要务，是让
这种退化解**在结构上不可能出现**，而不是靠一个惩罚项去压制它。

---

## 2. 我们加了什么

### 2.1 可学习的宏

我们引入**宏（macro）**：一条宏是由**现有算子**构成的子树，叶子是"洞"（搜索在此
继续展开）或终结符（该分支就此闭合）。宏作为**一个产生式**提供给 A\*。

```json
{"name": "float_fields_delta_exp",
 "body": {"op": "split_float", "children": [
   {"op": "hole"},
   {"op": "diff_mod", "children": [{"op": "hole"}]},
   {"op": "hole"}]}}
```

这个定义带来三条性质，它们由构造保证，各有测试守护。

**宏不可能藏数据。** 宏体里只有算子与洞，没有载荷。上一节那个退化解在这里不是被
惩罚，而是**不可表示**——每个字节仍然只能经由终结符载荷输出，并被完整计入归档。

**宏不可能扩大可表达的程序集合。** 宏体的每个节点都要在它实际落到的位置通过
`legalProductions` 检查，位宽守卫、深度预算、`--disable-op` 全部照常生效。因此宏只
能构造出原语文法本身也能构造的程序。一次消融如果移除了 `zigzag`，也会连带移除所有
含 `zigzag` 的宏——这保证了原文中的算子消融实验在引入宏之后依然成立。

**宏不可能改变归档。** 引擎在**序列化之前**把宏展开成原语，所以 `.brv` schema、
字节码格式与解码器完全不变。带库与不带库写出的归档，在两者选中同一程序时**逐字节
相同**。解压端从不知道宏的存在，也不需要拿到规则库。

因此宏改变的只有一件事：**在有限的展开预算下，搜索能够到达哪些程序。**

### 2.2 外层循环

有了宏这个载体，我们在原有搜索之外加了一层循环：

```
内层    A* 在当前 DSL 中为每个 tensor 搜索程序        （原系统）
外层    挖掘 + 提议新宏 → 验证 → 度量 → 保留或拒绝    （新增）
```

外层由两个来源产生候选。**挖掘**是确定性的：统计已完成搜索中反复出现的子树，按它们
所支配的归档字节加权排序——这是程序合成里 library learning 的经典做法。**提议**由
一个语言模型完成：它看到测量证据与算子表，写出候选宏。

模型只负责提议。它不能读源码、不能新增原语、不能修改搜索代价、不能修改自己的
prompt；输出必须是受限 JSON，算子名与 arity 在解析期校验。决定去留的全部是确定性
的程序（见 3.2）。

### 2.3 与原系统的接口

| 位置 | 改动 |
| --- | --- |
| `src/macro.zig` | 新增。宏库的 schema、解析、校验，不含搜索逻辑 |
| `src/search.zig` | `fitMacroBody` 把宏体嫁接进一个洞；展开循环把每条宏作为一个产生式提供 |
| `src/report.zig` | 导出算子表供外部工具读取；逐 tensor 报告该计划由哪些宏构造 |
| `src/main.zig` | 所有接受搜索参数的命令新增 `--macros <library.json>` |
| `autodsl/` | 新增。外层循环的全部实现与测试 |
| `.brv` 格式、解码器 | **未改动** |

不给 `--macros` 时，系统行为与原系统完全一致。

---

## 3. 方法

### 3.1 宏在 A\* 中如何计费

这是整个设计里最关键、也最容易做错的一处。

一条宏应该按什么代价加入优先队列？我们最初按"它展开后各节点代价之和"计费，理由是宏
不该获得未挣得的排序优势。**这个理由是错的。**

A\* 按 `f` 值展开。一条两节点的宏若按 `2 × floor` 计费，就排在**整个单产生式前沿
之后**；而那个前沿中每弹出一个节点，又会压入约 15 个子节点。在 256 次展开的预算
内，宏永远轮不到被弹出。

正确的读法也更符合 MDL 的本意：**宏是扩展文法 `G ∪ M` 中的一个符号，因此计一次
产生式**。这正是"抽象"的定义——它缩短了使用它的程序的描述长度。字节目标
`g_bytes + lowerBound` 在两种计费下都完全不变，所以原系统"两种代价相互独立"这条
不变量依然成立。4.1 给出这一处改动的实测影响。

### 3.2 六道门禁

一条候选宏必须依次通过下列检查，按代价从低到高排列，第一个失败即停：

1. **结构** — arity 正确、算子已知、不含宽度相关 arity 的算子、名字与形状唯一。
   失败时不启动任何进程。
2. **引擎接受** — 库能被加载，且每条宏都存活到解析完成。
3. **触发** — 确实有 tensor 选中了这条宏所构造的程序。未触发的宏不是中性的：它在
   每个洞都被提供，会稀释固定的展开预算。
4. **逐比特正确** — 写出真实的 `.brv` 并 `brevis verify`。宏由已验证可逆的算子组合
   而成，这一关本不该失败，正因如此它值得跑：一次失败意味着某条假设破了。
5. **有收益** — 在 develop 集上的降幅超过阈值，在 validation 集上的涨幅在容差内，
   规划耗时增幅不超过 1.25×。
6. **不毁掉任何单个模型** — 单个文件的损失不得超过其自身归档的 0.2%。

第 6 条是被数据逼出来的。某次运行语料总量减少 7.36 MB，其中一个 Qwen 分片却**大了
4.43 MB**。只看总和的门禁会放行这种结果。

### 3.3 目标函数

自扩展语法的教科书式 MDL 目标是

```
minimize  |Serialize(L)| + Σᵢ |Serialize(Pᵢ | L)|
```

在 Brevis 上实测，这两项都是噪声：程序字节码占归档 **0.13%**，而规则库因为在序列化
前就被展开，对归档的贡献是 **0**。优化它们等于优化一个舍入误差。

规则库真正消耗的是**搜索预算**——每条宏在每个洞都被提供，库越大，固定预算内探索到
的不同结构越少。因此我们实际执行的目标是

```
minimize  Σᵢ archive_bytes(tensorᵢ | L) + |Serialize(L)|
subject to  逐比特可解码，且规划耗时在 1.25× 以内
```

`|Serialize(L)|` 仍然照收，尽管它可忽略：一是若将来规则库需要随归档分发，它会变成
约束项；二是一个关于 MDL 的主张，应当把自己声称可忽略的那一项算出来给人看。

### 3.4 数据划分

我们最初只有 develop / holdout 两层，而 holdout **参与了每一个候选的接受决策**
（作为"不得回退"的否决票，约十二次），并被最终判定读过两次。那是 validation，不是
test。现在是三层，由代码强制：

| 层 | 模型 | 角色 |
| --- | --- | --- |
| develop | SmolLM (I8+BF16)、BERT (F32)、ViT (F32) | 被挖掘；**提议器只看到其中一个**；必须改善 |
| validation | TinyLlama (BF16) | 接受时的否决票，因而属于训练回路 |
| test | Whisper (F16)、SDXL (F16, 四组件)、Qwen3-8B (BF16, 五分片) | 只由最终判定读取，每次接触追加到 `runs/test-log.jsonl` |

"提议器只能看到一个 develop checkpoint"现在是一个**可测试的性质**，而不是一个约定。

---

## 4. 实验

所有归档字节均为确定性测量：搜索与编解码中不含随机数，重复运行以及**跨线程数**都
逐字节相同。因此任何非零差值都是信号而非噪声。

### 4.1 计费方式决定了宏会不会被用到

在 SmolLM 上使用同一个宏库，只改变 3.1 中的计费方式：

| 计费方式 | 归档字节 | 相对无宏 | 选中宏所构造程序的 tensor |
| --- | ---: | ---: | ---: |
| 按展开节点数 | 171,932,437 | **+92,674** | **0 / 483** |
| 按一次产生式 | 171,835,441 | **−3,988** | **210 / 483** |

在修正之前，所有宏——包括模型提出的三条结构合法且合理的提议——**从未被选中过一
次**。表面现象是"宏没有用"，实际原因是计费方式让它们在预算内不可达。这是整个工作
中影响最大的单个发现，也说明"宏是文法中的一个符号"这一读法不只是形式上的优雅。

### 4.2 宏带来的是搜索加速还是可达性

我们对原始 DSL 与宏 DSL 分别扫描 64…4096 次展开预算。这个实验用来判定贡献的**性
质**：若原始 DSL 加大预算后也能找到同样的程序，宏只是搜索加速；若在同等甚至更大的
wall-time 下原始 DSL 仍然找不到，宏就是更强的可达性抽象。

| 模型 | 原始 DSL 扫描内最优 | 宏 DSL @256 | 判定 |
| --- | ---: | ---: | --- |
| SmolLM (I8+BF16) | 171,835,356 @512 | **171,726,489** | 4096 次内从未追平 → 可达性 |
| BERT (F32) | 365,681,984 @4096 | 366,000,885 | @512 追平，且 wall-time 更低 → 至多是加速 |
| ViT (F32) | 288,079,353 @512 | 288,080,233 | 同上 |

两点值得单独指出。**低预算下效应很大**：在 64 次展开时，宏使 BERT 减少 10.9%、
ViT 减少 10.6%。**更多搜索并不单调**：原始 DSL 从 512 增到 2048 次展开时，在 SmolLM
与 ViT 上归档反而变大，因此"给基线更多预算"并不是一个干净的对照。

### 4.3 收益是广泛的还是集中的

逐张量对比带宏与不带宏的归档字节：

| 模型 | dtype | 宏触发的张量 | 改善/变差 | 改善中位数 | 前 5 名占收益 | 总计 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| TinyLlama | BF16 | **174 / 201** | 175 / 15 | **8,094 B** | **15.2%** | −2,121,427 |
| SmolLM | I8+BF16 | 212 / 483 | 196 / 5 | 7 B | 98.9% | −112,940 |
| BERT | F32 | **0** | 166 / 31 | 7 B | 46.2% | −5,984 |
| ViT | F32 | **0** | 156 / 34 | 7 B | 26.6% | −5,070 |

只有在纯 BF16 模型上收益是广泛的：174 个张量选中宏所构造的程序，改善张量的中位收益
是 8 KB，最差回退仅 107 字节。在两个 F32 模型上宏**从不触发**，那里的微小差值是候选
重排序的副作用，并且分别有 31 与 34 个张量反而变差。I8 张量的收益恰好为零。

因此我们学到的是**一条 BF16 浮点字段规则**，不是一条通用规则。

### 4.4 冻结测试集上的结果

三个从未被挖掘、从未被提议器看到、从未参与任何接受决策的 checkpoint 家族：
Whisper（F16，语音）、SDXL（F16，扩散，四个组件）、Qwen3-8B（BF16，五个分片），
共 10 个文件 26.4 GB。使用 4.5 选出的两条宏：

```
基线      19,390,217,369 字节
净减           8,608,409 字节 = 0.0444%（已扣除规则库序列化开销）
逐比特      10 / 10 个文件：写真实 .brv → verify → 全量解压比对
文件分布    7 / 10 个文件变小，最差的单个文件增大 4,131,518 字节
规划耗时    0.98×（快于不带宏）
```

回退的分布值得单独看。在一个更早的两条宏的库上我们有完整的逐文件数据，模式是一致
的：变差集中在少数文件，且都是把某个大张量推到了更差的程序上。

| 文件（早期两条宏的库） | Δ | 最差单张量 |
| --- | ---: | ---: |
| Qwen3-8B 分片 1 | +4,429,751 | +2,179,130 |
| Whisper (F16) | +165,305 | +258,575 |
| SDXL text_encoder_2 | +91,792 | +127,323 |
| 其余 7 个文件 | −13,295,257 | — |

### 4.5 这个方向的上界

前面的实验回答"某个流程有多好"，这一个回答"总共能拿到多少"。

做法是刻意作弊：把本项目**曾经产生过的全部 9 条宏**（挖掘得到的、模型提议的、变异
得到的，包括那些所在库后来被否决的）并成一个候选池，直接做贪心前向选择——**不需要
任何搜索去找，等于把答案交到手上**。这是"宏级自扩展在当前算子集上"的乐观上界。

前向选择停在两条宏：

```
split_field(rans, split_field(rans, bitpack))      [模型提议]
split_float(?, rans, split_field(?, bitpack))      [变异得到]
```

在冻结测试集上：

| 库 | Δ | 占上界 |
| --- | ---: | ---: |
| **上界：全部宏的最优子集** | **−8,608,409** | **100.0%** |
| 自动流程（贪心接受，运行 A） | −8,430,499 | 97.9% |
| 自动流程（种群搜索，运行 B） | −8,430,498 | 97.9% |
| 自动流程（种群搜索，运行 C） | −7,379,859 | 85.7% |
| 自动流程（贪心接受，运行 D） | −5,882,844 | 68.3% |
| 仅挖掘，不使用模型 | +7,954,920 | 比不做还差 |

两个结论。**自动流程已经逼近上界**：最好的运行达到 97.9%，即使把所有发现过的宏最优
组合，也只能再多 2.1%（约 180 KB）。**运行之间 68%–98% 的差异由"模型那一轮是否提出
了好宏"决定，而不是由搜索策略决定**——贪心与种群搜索都出现在 97.9% 一档，也都出现
在较差的一档。

最后一行同样重要：**只做挖掘、不使用模型，会让冻结测试集变差 7,954,920 字节。** 挖掘
出的高频子树在 develop 上有收益，却完全不迁移。这说明外层循环里真正起作用的是"提议
+ 验证"，而不是"统计高频结构"。

### 4.6 搜索策略本身的对照

带模型的运行之间存在一个致命问题：**同一个随机种子，贪心两次跑出 −38,042 和 +0**。
提议器的非确定性方差比被测效应本身还大，因此任何单次运行的策略对比都不构成证据。

为把这个方差移出比较，我们做了一个确定性对照：**给两个策略同一个固定的 9 宏池，
完全不调用模型**，它们只在"如何选择与组合"上不同。

| 策略 | develop Δ | validation Δ | 评估次数 | 宏数 |
| --- | ---: | ---: | ---: | ---: |
| 种群搜索（前向选择 + 组合 + 丢弃） | **−752,660** | **−2,290,547** | 30 | 3 |
| 贪心接受（逐条按序） | −743,193 | −2,125,307 | **9** | 2 |

种群搜索找到了最优解——**−752,660 正是 4.5 中上界库在 develop 上的取值**。贪心到达
其 98.7% 后**候选耗尽**，在第 9 次评估即停止，无法继续使用剩余预算。换一个随机种子
重跑，贪心给出完全相同的 −743,193，即固定池下它是确定性的。

逐轮轨迹是这个对照中信息量最大的部分：

```
种群搜索   825,893,591 → 825,275,840 → 825,178,989      三轮连续改善
                       ↓ −617,751    ↓ −96,851

贪心接受   825,915,159 ×3 → 825,188,456 ×6              一次跳跃后候选耗尽
```

**在测量有效的设定下，多轮细化确实存在**：种群搜索经三轮连续改善到达最优，贪心只有
一次改善。但最终差距只有 9,467 字节（1.3%），代价是 3.3 倍的评估预算。

我们还做了一个消融：**关闭模型后的种群搜索**花光 30 次评估预算，在 develop 上得到
−16,481；而**纯挖掘**是 −16,531。变异、交叉与随机采样在挖掘所得的种子上，最终落点
就是挖掘本身。这与 4.5 最后一行一致：价值来自模型的提议，搜索机制的贡献是"看得更
多、并拒绝偏科的宏"。

---

## 5. 结论与局限

**新增的这一层是有效的，并且效果有明确的上界。** 在三个从未接触过的 checkpoint 家族
上，学到的规则库使归档净减 0.044%，逐比特正确，规划耗时反而下降 2%。规则库本身不进
入归档，解码器不受任何影响，因此这一层是可以独立开关的增量。

**这个方向的上界就是 0.044%，而自动流程已经覆盖其 97.9%。** 我们认为这本身是一个值
得报告的结果：它说明限制因素是**算子集的表达能力**，而不是搜索或提议策略。宏重排的
是"有限搜索能到达哪个程序"，无法改变占归档 99.87% 的残差比特如何建模。要继续推进，
需要新增可逆原语（`forward`/`inverse` 对），那是另一类工作，带有真正的可逆性证明
义务，不应交给自动门禁去决定。

局限如下：

1. **学到的是 BF16 浮点字段规则，不是通用规则。** 在两个 F32 模型上宏从不触发；在
   F16 上使 Whisper 变差 165,305 字节。冻结测试集的 10 个文件中有 3 个变差，最差
   单文件增大 4,131,518 字节。
2. **冻结测试集只有 3 个模型家族、10 个文件。** 尚未覆盖 MoE、FP8、optimizer state
   与非权重张量。
3. **搜索策略的增量很小。** 受控对照下种群搜索相对贪心接受的最终优势为 1.3%，代价
   是 3.3 倍评估预算；带模型时两者的差异被提议器方差淹没。若要给出有说服力的方差
   估计，需要每个策略重复十次以上。
4. **本文的数字是开发期测量，不是 `eval/` 的 audited 结果。** 正式发表前需要走
   `eval/` 的两步式 formal 流程重新测量。

---

## 6. 复现信息

| | |
| --- | --- |
| 分支 | `autoDSL` |
| 工具链 | Zig 0.16.0，Python 3.12.4，`source setup_env.sh` |
| 节点 | c170.nibi.sharcnet，Xeon 6972P，`--jobs 12` |
| 二进制 | sha256 `583ac71fd32c1de0c7be77aca3c1ed574323a1bc6cbd36cc407a9a5fe1772f15` |
| checkpoint | repo、revision 与逐文件 sha256 固定在 `eval/models-tiered.json` |
| 测试 | 45 项 Zig，190 项 Python（`autodsl/`），13 项（`tools/`），全部通过 |
| 冻结测试集接触记录 | `autodsl/runs/test-log.jsonl`，每次读取均有记录 |

```bash
source setup_env.sh && zig build -Doptimize=ReleaseFast
cd autodsl
python3 loop.py bootstrap --top 8                 # 纯挖掘，不调用模型
python3 loop.py propose --rounds 1 --wanted 3     # 模型提议一轮
python3 loop.py verdict --tier test               # 冻结测试集判定
python3 experiments.py --run exp budget-curve     # 4.2
python3 experiments.py --run exp per-tensor       # 4.3
python3 ceiling.py                                # 4.5
python3 search.py --run controlled --pool runs/pool.json --arms greedy,search   # 4.6
```

关于成本口径，避免"规划耗时 0.98×"被读成免费：宏在 A\* 中只花一次弹出，但
`fitMacroBody` 需要对宏体的每个节点执行一次 `ops.forward` 并建立直方图，单次弹出的
代价高于原语。0.98× 是**已经付过这笔代价之后**的数字。规则发现阶段是离线的，不计入
上述测量：约 40 次引擎调用、数次模型调用、数小时 wall clock。
